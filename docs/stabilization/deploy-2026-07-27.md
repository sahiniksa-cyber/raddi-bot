# Deploy note — 2026-07-27

Production runs the `production` branch = `master` (working reply system) + the
WhatsApp QR version fix (fetchLatestWaWebVersion).

This branch does NOT contain the codex canonical-merchant-policy enforcement,
so `MERCHANT_POLICY_MISSING` cannot occur — AI replies use the legacy config.

Rollback: redeploy the previous deployment (db124875 / 7d854fd) in Railway.
