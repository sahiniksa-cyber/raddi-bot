---
tags: [قضية-مفتوحة, واتساب, تحقيق]
التاريخ: 2026-07-29
الحالة: مفتوحة
---

# ربط التاجرة — تحقيق 2026-07-29

## الوضع
التاجرة **`+966537720085`** (userId `d579ac09`) تحاول تربط جهازها، وتقول "تعذّر ربط الجهاز" ~١٠ مرات.

## اللي تأكّد بالأدلة (لوقات الإنتاج + قراءة الكود)
1. **كود الباركود سليم ١٠٠٪.** إثبات قاطع: حساب `+966562529945` (`b23b0920`) **ربط بنجاح بالمسح** يوم 2026-07-28 الساعة 19:31 على نفس النسخة الحيّة `c521b34` (`isNewLogin → 515 → connected`). فمسار مسح الباركود يعمل فعلاً على المنصة.
2. **خدمة الباركود ترسم الـ ref الحيّ حرفياً** (`appState.qrString = this.connection.qr` تُقرأ لحظياً) — لا يوجد "باركود قديم مخدوم".
3. **مسح ناجح يُنتج `515` مو `428`.** لأن الجلسات الفاشلة تظهر `428` فقط، فالمسح **لا يصل السوكِت الحيّ** — وهذا مسؤولية سيرفر واتساب، لا كودنا.
4. **جلسة التاجرة `d579ac09` لم تُشغَّل ولا مرة اليوم** — لا "تشغيل" يدوي، لا باركود، لا محاولة مسح وصلت الخادم لها.

## خطأ تشخيصي صار وتصحّح
بالبداية رُوقبت الجلسة الغلط **`e1818f76` / `+97430529327`** (رقم قطري، حساب مختلف)؛ التاجرة الحقيقية **`+966537720085`**.

## الاستنتاج الأرجح
محاولات التاجرة **ما تصير على جلستها الحيّة**. الأرجح تمسح **باركود مجمّد/صفحة قديمة** أو **حساب متوقف/غلط**، فكل مسحة تضرب ref ميت → "تعذّر ربط الجهاز". (احتمال ثانوي: نمط "اتصل ثم انفصل بعد ٢٢ث" مثل `b23b0920`.)

## نمط جانبي يستحق المتابعة
حساب `b23b0920` (`+966562529945`) اتصل بنجاح ثم **انفصل بعد ٢٢ ثانية** (`logged out` + `unlink-alert`). لو التاجرة تمرّ بنفسه، نعالجه بعد ما نتأكد إنها توصل جلسة حيّة.

## الخطوة الحاسمة القادمة (لم تُنفَّذ)
اختبار حيّ مُراقَب على الجلسة الصحيحة:
1. التاجرة تدخل حسابها الصحيح (الصفحة تعرض `+966537720085`).
2. تسكّر الصفحة وتفتحها من جديد.
3. تضغط **تشغيل** → تنتظر باركود جديد → تمسحه فوراً.
4. نراقب لوق `d579ac09` مباشرة ونشوف: هل وصل المسح؟ اتصل؟ صمد أو انفصل؟

**نتيجة الاختبار تحسم:** لو ما وصل شي = مشكلة صفحة/حساب/جهاز؛ لو وصل وفشل بكود = مصافحة/نسخة نعالجها بالكود.

## الأسئلة المعلّقة
- لمّا تفتح التاجرة jwap.net، وش الرقم اللي يظهر لها؟ (لتأكيد إنها على الحساب الصحيح.)

مرتبط: [[كيف-يربط-التاجر-جهازه]] · [[قصة-مشكلة-الباركود]] · [[أوامر-مفيدة]]
