---
tags: [ملاحظات, سجل, تاريخ]
---

# سجل الجلسات (مختصر)

سجل زمني مختصر لأهم جلسات العمل والقرارات. الأحدث فوق.

## 2026-08-18 — تنبيه فصل الواتساب + حقيقة الاتصال (منشور)
- **مشكلتان:** الداشبورد يظهر «شغّال» كذبًا ولا يكشف الحاجة لباركود إلا بضغط زر؛ وتنبيه الفصل لا يصل لإدارة المنصة.
- **جذر مثبت بلوق production:** الفصل `code=401 loggedOut` نهائي؛ التنبيه انطلق بـ`channels=[NONE]` (وجهته كانت env غير مضبوط).
- **الحل:** مصنّف حالة اتصال حقيقي + شاشة QR_REQUIRED فورية؛ خانتا «رقم جوال تنبيهات المنصة» و«رابط المنصة» في لوحة الأدمن؛ تنبيه مبسّط عبر بوت الأدمن المستقل مع dedup (`health_incidents`) و**retry حتى التسليم** على tick الـhealth monitor. صفر مساس auth/session/pairing.
- **PR #184 مدموج ومنشور** → production HEAD `63d9d97`، deploy SUCCESS، إقلاع سليم. المتبقّي: المالك يدخل الرقم والرابط في اللوحة. التفاصيل: [[تنبيه-فصل-الواتساب-وحقيقة-الاتصال-2026-08-18]].

## 2026-07-29 — تحقيق ربط التاجرة + بناء هذا الدفتر
- فُحص كود الإنتاج بعمق (توليد/خدمة الباركود، إكمال الربط، حفظ الاعتماد).
- **تأكّد إن مسار مسح الباركود يعمل** (حساب `+966562529945` ربط بنجاح 2026-07-28 19:31).
- **تصحيح:** التاجرة الحقيقية `+966537720085` (`d579ac09`)، لا `+974...`.
- جلسة التاجرة لم تُشغَّل اليوم — الأرجح تمسح باركود مجمّد/حساب غلط. التفاصيل: [[ربط-التاجرة-2026-07-29]].
- أُنشئ **Obsidian vault** في `docs/vault/`.

## 2026-07-27 → 07-28 — إصلاح الباركود (٣ أسباب جذرية)
- ① نسخة WA Web قديمة → `4296c4e`.
- ② نافذة باركود ميتة 428 → `b72aec3`.
- ③ عاصفة ربط متزامنة → `c521b34` (الأحدث الحيّ).
- إثبات معزول عبر [[الروابط-والحسابات|wa-iso-probe]]. التفاصيل: [[قصة-مشكلة-الباركود]].

## قبلها (من الذاكرة العامة — مختصر)
- بناء عقل البوت، نظام الحصص (Quota)، إخفاء lid، متغيّرات المنتجات.
- استقرار الاتصال (lease / single-flight / boot-recovery / signal-keys).
- جذر Bad MAC (getMessage + throttle + keystore debounce).
- ميزة تعديل البرومنت من قروب التصعيد.
- وحدة ردود إنستقرام معزولة (خلف `INSTAGRAM_ENABLED`).
- حادثة امتلاء قرص Postgres (2026-07-07) → Resize + تنظيف جدول `messages`.

> [!note] كيف تحدّث هذا السجل
> كل جلسة عمل مهمة، أضف سطر تحت "الأحدث فوق" بالتاريخ + ملخص + روابط للصفحات المعنية.
