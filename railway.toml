[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node scripts/wa-isolation-service.js"
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 100
healthcheckPath = "/healthz"
healthcheckTimeout = 300
